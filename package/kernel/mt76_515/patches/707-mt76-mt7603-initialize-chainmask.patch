--- a/mt7603/eeprom.c
+++ b/mt7603/eeprom.c
@@ -185,6 +185,7 @@ int mt7603_eeprom_init(struct mt7603_dev
 	    is_mt7688(dev))
 		dev->mphy.antenna_mask = 1;
 
+	dev->mphy.chainmask = dev->mphy.antenna_mask;
 	mt76_eeprom_override(&dev->mphy);
 
 	return 0;
